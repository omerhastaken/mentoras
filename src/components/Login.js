import React from "react";
import styled from "styled-components";

const Login = () => {
  return (
    <Container>
      <h1>MENTORAS LOGIN PAGE</h1>
    </Container>
  );
};

const Container = styled.div``;

export default Login;
